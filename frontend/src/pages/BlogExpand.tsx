import { useParams } from "react-router-dom"

export default function BlogExpand(){
  return (
    <div className="flex justify-center items-center h-screen">
        Landed on expanded blog page!
    </div>
  )
}
